// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 3a82d651856f

package grants

import (
	"fmt"
	"github.com/paygentic/cli/internal/client"
	"github.com/paygentic/cli/internal/flagutil"
	"github.com/paygentic/cli/internal/interactive"
	"github.com/paygentic/cli/internal/output"
	"github.com/paygentic/cli/internal/sdk"
	"github.com/paygentic/cli/internal/sdk/models/operations"
	"github.com/paygentic/cli/internal/usage"
	"github.com/spf13/cobra"
)

var createCmdMeta = []flagutil.FlagMeta{
	{FlagName: "entitlement-id", FieldPath: "EntitlementID", Kind: flagutil.FlagKindString, Required: true, Description: "The unique identifier of the entitlement to grant credits to. [required]"},
	{FlagName: "amount", Shorthand: "a", FieldPath: "Body.Amount", Kind: flagutil.FlagKindFloat64, Required: true, Description: "The number of credits to grant. [required]"},
	{FlagName: "effective-at", FieldPath: "Body.EffectiveAt", Kind: flagutil.FlagKindDateTime, Optional: true, Description: "When the grant becomes effective. Defaults to now."},
	{FlagName: "expires-at", FieldPath: "Body.ExpiresAt", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"expiresAt,omitempty"`, Description: "When the grant expires. If omitted, the grant does not expire."},
	{FlagName: "idempotency-key", Shorthand: "i", FieldPath: "Body.IdempotencyKey", Kind: flagutil.FlagKindString, Required: true, Description: "Idempotency key to prevent duplicate grants. Must be unique per entitlement. [required]"},
}

// initCreateCmd initializes the create command.
func initCreateCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "create",
		Short:   "Create Grant",
		Long:    "Create a grant directly for a metered entitlement, crediting the customer's balance immediately. The entitlement must belong to an active v1 subscription.",
		Example: "  paygentic grants create --entitlement-id <id> --amount 100 --idempotency-key grant-initial-100",
		RunE:    runCreateCmd,
	}
	flagutil.RegisterFlags(cmd, createCmdMeta)
	if err := flagutil.ValidateMeta[operations.CreateEntitlementGrantRequest](createCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for create: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runCreateCmd executes the create command.
func runCreateCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, createCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, createCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.CreateEntitlementGrantRequest](cmd, createCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Entitlements.Grants.Create(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
