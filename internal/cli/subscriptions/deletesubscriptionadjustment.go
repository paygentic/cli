// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 783ee85a99cb

package subscriptions

import (
	"fmt"
	"github.com/paygentic/cli/internal/client"
	"github.com/paygentic/cli/internal/flagutil"
	"github.com/paygentic/cli/internal/interactive"
	"github.com/paygentic/cli/internal/output"
	"github.com/paygentic/cli/internal/sdk"
	"github.com/paygentic/cli/internal/sdk/models/operations"
	"github.com/paygentic/cli/internal/usage"
	"github.com/spf13/cobra"
)

var deleteSubscriptionAdjustmentCmdMeta = []flagutil.FlagMeta{
	{FlagName: "id", Shorthand: "i", FieldPath: "ID", Kind: flagutil.FlagKindString, Required: true, Description: "The subscription ID [required]"},
	{FlagName: "adjustment-id", Shorthand: "a", FieldPath: "AdjustmentID", Kind: flagutil.FlagKindString, Required: true, Description: "The adjustment ID [required]"},
}

// initDeleteSubscriptionAdjustmentCmd initializes the delete-subscription-adjustment command.
func initDeleteSubscriptionAdjustmentCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "delete-subscription-adjustment",
		Short:   "Delete Adjustment",
		Long:    "Deletes an adjustment that has not yet reached an issued invoice. No invoice changes: an invoice still in draft keeps its numbers, and loses the discount only when its period is calculated again. An adjustment that has already discounted an issued invoice cannot be deleted, because the invoice records why the customer was charged that amount. Its window cannot be shortened afterwards either, so set effectiveTo at creation time whenever the deal has a known end date.",
		Example: "  paygentic subscriptions delete-subscription-adjustment --id <id> --adjustment-id <id>",
		RunE:    runDeleteSubscriptionAdjustmentCmd,
		Aliases: []string{"dsa"},
	}
	flagutil.RegisterFlags(cmd, deleteSubscriptionAdjustmentCmdMeta)
	if err := flagutil.ValidateMeta[operations.DeleteSubscriptionAdjustmentRequest](deleteSubscriptionAdjustmentCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for delete-subscription-adjustment: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runDeleteSubscriptionAdjustmentCmd executes the delete-subscription-adjustment command.
func runDeleteSubscriptionAdjustmentCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, deleteSubscriptionAdjustmentCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, deleteSubscriptionAdjustmentCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.DeleteSubscriptionAdjustmentRequest](cmd, deleteSubscriptionAdjustmentCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Subscriptions.DeleteSubscriptionAdjustment(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
