// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 2a327aff8d54

package subscriptions

import (
	"fmt"
	"github.com/paygentic/cli/internal/client"
	"github.com/paygentic/cli/internal/flagutil"
	"github.com/paygentic/cli/internal/interactive"
	"github.com/paygentic/cli/internal/output"
	"github.com/paygentic/cli/internal/sdk"
	"github.com/paygentic/cli/internal/sdk/models/operations"
	"github.com/paygentic/cli/internal/usage"
	"github.com/spf13/cobra"
)

var updateSubscriptionCmdMeta = []flagutil.FlagMeta{
	{FlagName: "id", Shorthand: "i", FieldPath: "ID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "ending-at", Shorthand: "e", FieldPath: "Body.EndingAt", Kind: flagutil.FlagKindDateTime, Optional: true, Description: "date/time value"},
	{FlagName: "status", Shorthand: "s", FieldPath: "Body.Status", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"active", "terminated"}, Description: "options: active, terminated"},
	{FlagName: "terminated-at", FieldPath: "Body.TerminatedAt", Kind: flagutil.FlagKindDateTime, Optional: true, Description: "Effective termination timestamp. Capped at the current effective time (future values are clamped). Must be strictly after the subscription's start date — values at or before startedAt are rejected with 400."},
	{FlagName: "terminated-by", FieldPath: "Body.TerminatedBy", Kind: flagutil.FlagKindString, Optional: true, Description: "Identifier of entity that cancelled the subscription. Sample values: 'cust_abc123' for customer-initiated cancellation, 'org_xyz789' for merchant-initiated cancellation"},
	{FlagName: "termination-reason", FieldPath: "Body.TerminationReason", Kind: flagutil.FlagKindString, Optional: true, Description: "Explanation for subscription cancellation. Sample values: 'Customer requested cancellation', 'Payment failure', 'Service migration', 'Contract expiration'"},
	{FlagName: "auto-charge", Shorthand: "a", FieldPath: "Body.AutoCharge", Kind: flagutil.FlagKindBool, Optional: true, Description: "Enable or disable automatic charging of invoices using stored payment methods."},
	{FlagName: "tax-exempt", FieldPath: "Body.TaxExempt", Kind: flagutil.FlagKindBool, Optional: true, Description: "When true, forces tax rate to 0%."},
	{FlagName: "minimum-account-balance", Shorthand: "m", FieldPath: "Body.MinimumAccountBalance", Kind: flagutil.FlagKindString, Optional: true, Description: "Minimum wallet balance requirement in decimal dollars (e.g., '100.00'). Can be set to '0' to disable. Maximum allowed is $5,000. Contact support for higher limits. Note: If the calculated charge amount at renewal is below payment processor minimums (typically $1.00), it will be automatically adjusted upward to meet the minimum requirement. Changes apply at next renewal."},
	{FlagName: "renewal-reminder-enabled", FieldPath: "Body.RenewalReminderEnabled", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"renewalReminderEnabled,omitempty"`, Description: "Override plan setting for renewal reminder emails. Set to true to enable, false to disable, or null to use plan default."},
	{FlagName: "renewal-reminder-days", FieldPath: "Body.RenewalReminderDays", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"renewalReminderDays,omitempty"`, Description: "Override plan setting for number of days before renewal to send the reminder. Set to null to use plan default."},
	{FlagName: "payment-term-days", Shorthand: "p", FieldPath: "Body.PaymentTermDays", Kind: flagutil.FlagKindInt64, Optional: true, Description: "Payment term in days (\"Net X\") applied to subsequently generated invoices: invoice dueAt = invoice issue date + paymentTermDays. A non-zero value is only valid alongside bankTransferOnly=true. Set 0 for \"due on issue\". Already-issued invoices keep their snapshotted dueAt."},
}

// initUpdateSubscriptionCmd initializes the update-subscription command.
func initUpdateSubscriptionCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "update",
		Short:   "Update",
		Long:    "Update",
		Example: "  paygentic subscriptions update --id <id>",
		RunE:    runUpdateSubscriptionCmd,
	}
	flagutil.RegisterFlags(cmd, updateSubscriptionCmdMeta)
	if err := flagutil.ValidateMeta[operations.UpdateSubscriptionRequest](updateSubscriptionCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for update-subscription: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runUpdateSubscriptionCmd executes the update-subscription command.
func runUpdateSubscriptionCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, updateSubscriptionCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, updateSubscriptionCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.UpdateSubscriptionRequest](cmd, updateSubscriptionCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Subscriptions.UpdateSubscription(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
