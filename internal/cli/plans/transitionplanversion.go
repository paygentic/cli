// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 7125c00c576d

package plans

import (
	"fmt"
	"github.com/paygentic/cli/internal/client"
	"github.com/paygentic/cli/internal/flagutil"
	"github.com/paygentic/cli/internal/interactive"
	"github.com/paygentic/cli/internal/output"
	"github.com/paygentic/cli/internal/sdk"
	"github.com/paygentic/cli/internal/sdk/models/operations"
	"github.com/paygentic/cli/internal/usage"
	"github.com/spf13/cobra"
)

var transitionPlanVersionCmdMeta = []flagutil.FlagMeta{
	{FlagName: "id", Shorthand: "i", FieldPath: "ID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "version-number", Shorthand: "v", FieldPath: "VersionNumber", Kind: flagutil.FlagKindInt64, Required: true, Description: "The version number within the plan (1-based). [required]"},
	{FlagName: "default", FieldPath: "Body.Default", Kind: flagutil.FlagKindBool, Optional: true, Description: "Set to true to point the plan's default at this version. Idempotent on the already-default version."},
}

// initTransitionPlanVersionCmd initializes the transition-plan-version command.
func initTransitionPlanVersionCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "transition-plan-version",
		Short:   "Set the default version",
		Long:    "Point the plan's default at this version, so its floating subscriptions bill from it. Any version may become default, in either direction — this is how you roll a plan back to (or forward to) an earlier price set. Idempotent when the version is already the default.",
		Example: "  paygentic plans transition-plan-version --id <id> --version-number 414716",
		RunE:    runTransitionPlanVersionCmd,
		Aliases: []string{"tpv"},
	}
	flagutil.RegisterFlags(cmd, transitionPlanVersionCmdMeta)
	if err := flagutil.ValidateMeta[operations.TransitionPlanVersionRequest](transitionPlanVersionCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for transition-plan-version: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runTransitionPlanVersionCmd executes the transition-plan-version command.
func runTransitionPlanVersionCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, transitionPlanVersionCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, transitionPlanVersionCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.TransitionPlanVersionRequest](cmd, transitionPlanVersionCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Plans.TransitionPlanVersion(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
